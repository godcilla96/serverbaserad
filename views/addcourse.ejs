<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moment 1 - serverbaserad webbutveckling</title>
    <link rel = "stylesheet" href = "/css/style.css">
</head>
<body>
    <nav>
        <ul>
            <li><a href = "/">Visa kurser</a></li>
            <li><a href = "/addcourse" class = "current">Lägg till kurs</a></li>
            <li><a href = "/about">Om</a></li>
        </ul>
    </nav>
    <main>
    <h1>Lägg till kurs</h1>
    <p>Här kan du lägga till nya kurser</p>
    <form action="/add-course" method="POST">
        <label for="coursenames">Kursnamn:</label>
        <input list="coursename" id="courses" name="coursename" required>
        <datalist id="coursename">
            <% courses.forEach(course => { %>
                <option value="<%= course.KURSNAMN %>">
            <% }); %>
        </datalist>
        
        <label for="coursecodes">Kurskod:</label>
        <input list="coursecode" id="codes" name="coursecode" required>
        <datalist id="coursecode">
            <% courses.forEach(course => { %>
                <option value="<%= course.KURSKOD %>">
            <% }); %>
        </datalist>
        
        <label for="progressions">Progression:</label>
        <input list="progression" id="progressions" name="progression" required>
        <datalist id="progression">
            <% courses.forEach(course => { %>
                <option value="<%= course.PROGRESSION %>">
            <% }); %>
        </datalist>
        
        <label for="syllabuses">Kursplan:</label>
        <input list="syllabus" id="syllabuses" name="syllabus" required>
        <datalist id="syllabus">
            <% courses.forEach(course => { %>
                <option value="<%= course.KURSPLAN %>">
            <% }); %>
        </datalist>
        
        <input type="submit" value="Lägg till kurs" class="submit">
    </form>
    </main>
    <footer>
        Skapad av Cecilia Lindström celi2302@student.miun.se
    </footer>

</body>
</html>